export interface IStorageServices {
    STORAGE_SERVICES: {
        SMART_TASKS: string;
        LOCALIZATION: string;
        TRANSLATION: string;
        THEME: string;
        ALL_THEMES: string;
        USER: string;
    }
}
